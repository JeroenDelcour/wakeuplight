# Dev setup

Use [rshell](https://github.com/dhylands/rshell) (pip install rshell) to move files to/from the device. `rshell -p /dev/ttyUSB0` to connect through USB. `cp *.py /pyboard/` to copy files to device.